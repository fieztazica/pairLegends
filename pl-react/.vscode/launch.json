{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "msedge",
      "request": "launch",
      "name": "localhost (Edge)",
      "url": "https://localhost:3000",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
